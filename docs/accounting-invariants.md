# Formal Accounting Invariants
## Savings Vault Contract

---
## Overview
This document defines the formal accounting invariants that must *always* hold true for the Savings Vault contract, serving as audit preparation and a reference for developers.

---
## 1. Core Balance Invariants

### Invariant 1.1: Individual User Balance Conservation
For every user address U at any time T:
```
available_balance(U, T) + locked_balance(U, T) = net_deposited(U, T)
```
Where:
- `available_balance(U, T)`: [get_balance(env, U)](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/lib.rs#L629) – available balance (deposited + matured locks)
- `locked_balance(U, T)`: [get_locked_balance(env, U)](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/lib.rs#L805) – sum of all unmatured lock amounts
- `net_deposited(U, T)`: total amount deposited by user U minus total amount withdrawn by user U up to time T

**Tests Covering This Invariant**:
- [balance_conservation.rs](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/test/balance_conservation.rs)
- [property_vault_accounting.rs - prop_single_user_conservation](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/test/property_vault_accounting.rs#L339)
- [property_vault_accounting.rs - prop_near_max_deposit_no_overflow](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/test/property_vault_accounting.rs#L348)

---
### Invariant 1.2: Global Token Custody Invariant
For all times T:
```
contract_token_balance(T) = sum_over_all_users(available_balance(U, T) + locked_balance(U, T))
```
Where `contract_token_balance(T)` is the SAC token balance of the contract address at time T.

**Tests Covering This Invariant**:
- [property_vault_accounting.rs - prop_global_token_custody](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/test/property_vault_accounting.rs#L364)

---
## 2. Lock Entry Invariants

### Invariant 2.1: Lock Entry Amounts Are Positive
Every lock entry for every user has an amount > 0.

### Invariant 2.2: Lock IDs Are Monotonically Increasing (Per User)
Lock IDs generated by `lock_funds` are strictly increasing per user, starting at 1.

**Tests**: [property_vault_accounting.rs - all lock operations](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/test/property_vault_accounting.rs)

---
## 3. User Isolation Invariants

### Invariant 3.1: User Balance Isolation
Operations on user A's balance have *no effect* on any other user's balance, locks, or next lock ID.

**Tests**:
- [balance_conservation.rs - conservation_cross_user_isolation](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/test/balance_conservation.rs)
- [property_vault_accounting.rs - prop_cross_user_isolation](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/test/property_vault_accounting.rs#L359)

---
## 4. Failed Operations Invariants

### Invariant 4.1: Failed Operations Are No‑Ops
For any operation that fails (panics or errors), the state of the contract (all user balances, locks, storage) remains *exactly the same* as before the operation was invoked.

**Tests**:
- [balance_conservation.rs - conservation_invalid_deposits_do_not_mutate](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/test/balance_conservation.rs)
- [balance_conservation.rs - conservation_invalid_withdrawals_do_not_mutate](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/test/balance_conservation.rs)
- [balance_conservation.rs - conservation_invalid_locks_do_not_mutate](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/test/balance_conservation.rs)
- [property_vault_accounting.rs - failed operations checks](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/test/property_vault_accounting.rs)

---
## 5. Withdrawal Invariants

### Invariant 5.1: Withdrawal Amounts Are Bounded
A withdrawal of amount A from user U at time T is only allowed if:
```
0 < A ≤ available_balance(U, T)
```

### Invariant 5.2: Withdraw Lock Only Allowed on Matured Locks
A `withdraw_lock` operation on lock ID L for user U at time T is only allowed if:
- The lock entry L exists for user U
- `current_time >= unlock_time` of lock entry L

**Tests**: [withdraw_lock.rs](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/test/withdraw_lock.rs)

---
## 6. Initialization Invariants

### Invariant 6.1: Initialization Is Idempotent
The contract can be initialized *exactly once*; subsequent calls to `initialize` panic without changing state.

### Invariant 6.2: No Operations Before Initialization
All functions except `initialize` panic if called before the contract is initialized.

---
## 7. Admin Invariants

### Invariant 7.1: Admin Cannot Modify User Balances or Locks
The admin address cannot modify any user's balance, lock entries, or next lock ID counter.

**Tests**: [admin_invariant_guard.rs](file:///c:/Users/muham/.trae/Grantfox%20Coder%20x/pocketpay-contracts/contracts/savings_vault/src/test/admin_invariant_guard.rs)

### Invariant 7.2: Only Admin Can Transfer Admin Privileges
The `transfer_admin` function can only be called by the current admin, and requires admin authorization.
